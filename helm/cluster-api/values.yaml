name: cluster-api
registry:
  domain: quay.io
images:
  core:
    name: giantswarm/cluster-api-controller
  bootstrap:
    name: gcr.io/k8s-staging-cluster-api/kubeadm-bootstrap-controller
  controlplane:
    name: gcr.io/k8s-staging-cluster-api/kubeadm-control-plane-controller
  tag: v1.0.4-gs1
  bTag: nightly_main_20220404

project:
  branch: "[[ .Branch ]]"
  commit: "[[ .SHA ]]"

# We install CRDs through a Job with the helm specific crd folder.
crds:
  install: true
